{
  "name": "hello-prisma",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "generate": "prisma generate",
    "start": "ts-node test.ts",
    "reset": "rm -rf ./prisma/migrations &&  prisma migrate reset --force --preview-feature && prisma migrate dev --name init && cd .."
  },
  "devDependencies": {
    "@types/node": "^18.8.0",
    "prettier": "^2.7.1",
    "prisma": "^4.4.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@prisma/client": "4.4.0"
  }
}
